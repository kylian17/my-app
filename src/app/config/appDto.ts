import { entryDto } from "./entryDto";

export interface appDto {
    author?: object;
    entry?: entryDto[];
    icon?: object;
    id?: object;
    link?: object[];
    right?: object;
    title?: object;
    updated?: object;
}